// Entrypoint (run via `tsx src/index.ts`). Trivial in Phase 0 — proves the
// skeleton typechecks under strict→strictest. Real composition root lands later.
const main = (): void => {
  console.log("ynab-copilot: skeleton ready");
};

main();
